#ifndef MYLIB_H // prevents multiple inclusion
#define MYLIB_H //marks the file as “already included” so it doesn’t get included again and cause duplicate errors.

// 🔷 FUNCTION DECLARATIONS
namespace MyLib
{
    int add(int a, int b);
    int sub(int a, int b);
    int mul(int a, int b);
}
#endif // end of guard